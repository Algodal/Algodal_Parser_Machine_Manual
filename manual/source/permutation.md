# Permutation

A series can be parsed as a permutation using the `perm` command, which matches the members in *any order*. The series is wrapped in `[]`.

```parser
A = perm["A" "B" "C"];
```

:::{seealso}
For matching text characters in any order, see `tex::order` in [Text Function](text_function.md).
:::