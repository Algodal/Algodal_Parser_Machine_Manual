# Config Settings

Some settings can be configured from within the language itself using a `config` block.

```parser
config {
    "error-program-name": "spx-parser",
    "error-unknown-char-msg": "does not recognize character",
}
```

```
config { `<feature>` : `<value>` }
```

:::{note}
Still in development and not final
:::

